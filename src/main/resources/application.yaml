server:
  port: 1207
  servlet:
    context-path: "/employee"

spring:
  datasource:
    url: "jdbc:mysql://localhost"
    username: "root"
    password: "Atiba"

  sql:
    init:
      mode: ALWAYS

  jpa:
    properties:
      hibernate.format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: update

